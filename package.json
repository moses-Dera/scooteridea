{
  "name": "ebike-platform",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "backend/services/*",
    "backend/shared/*",
    "simulator",
    "frontend/*",
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^7.0.0"
  },
  "scripts": {
    "dev": "node run-dev.js",
    "dev:turbo": "turbo run dev --parallel",
    "build": "turbo run build",
    "start:prod": "turbo run start --parallel",
    "test": "turbo run test",
    "docker:up": "docker compose -f backend/infra/docker-compose.yml up -d postgres redis mosquitto",
    "docker:down": "docker compose -f backend/infra/docker-compose.yml down",
    "docker:clean": "docker compose -f backend/infra/docker-compose.yml down -v",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "turbo run lint"
  }
}
