{
  "name": "ebike-platform",
  "private": true,
  "workspaces": [
    "backend/services/*",
    "backend/shared/*",
    "simulator"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "scripts": {
    "dev":   "turbo run dev --parallel",
    "build": "turbo run build",
    "test":  "turbo run test"
  }
}
